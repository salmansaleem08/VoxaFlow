fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
httpx>=0.27.0
aiofiles>=23.2.1

# Telephony
twilio>=9.2.0

# LLM — Gemini (primary) + Groq (fallback)
google-genai>=1.0.0
groq>=0.9.0

# TTS
gTTS>=2.5.1

# STT — Deepgram nova-2 (cloud, telephony-optimized, uses httpx above)

# Async support
anyio>=4.4.0
